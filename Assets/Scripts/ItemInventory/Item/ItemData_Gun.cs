using UnityEngine;

[CreateAssetMenu(fileName = "New Item Data", menuName = "Data/Gun")]
public class ItemData_Gun : ItemData
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
